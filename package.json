{
  "name": "poetic-typewriter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:cloudflare": "vite build --mode cloudflare",
    "cloudflare:check": "npm run build:cloudflare && npm run cloudflare:check --prefix api",
    "deploy:cloudflare": "node scripts/deployCloudflare.js",
    "smoke:cloudflare": "node scripts/smokeTestDailyBrowser.js --cloudflare",
    "preview": "vite preview",
    "smoke:browser:daily": "node scripts/smokeTestDailyBrowser.js",
    "test": "node --test scripts/*.test.js"
  },
  "devDependencies": {
    "@fontsource/playfair-display": "^5.3.0",
    "@fontsource/special-elite": "^5.3.0",
    "autoprefixer": "^10.5.5",
    "playwright": "^1.55.0",
    "postcss": "^8.5.28",
    "tailwindcss": "^3.4.19",
    "vite": "^5.4.19"
  }
}
